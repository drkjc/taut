<%= form_for([@contact, @message]) do |f| %>
  <%= fields_for @contact.messages.build do |message_fields|  %>
    <%= message_fields.text_field :content %>
  <% end %>
  <%= f.submit 'Send' %>
<% end %>
