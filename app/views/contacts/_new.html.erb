<%= form_for ([@contact, @message]) do |f| %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :contact_id %>
  <%= f.text_field :content, placeholder: "message #{contact.username}" %>
  <%= f.submit 'Send' %>
<% end %>
