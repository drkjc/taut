<%= form_for([@found_group, @group_message]) do |f| %>
  <%= f.hidden_field :user_id %>
  <%= f.hidden_field :group_id %>
  <%= f.text_field :content, placeholder: "Message #{@found_group.name}" %>
  <%= f.submit 'send' %>
<% end %>
