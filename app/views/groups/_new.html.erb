<%= form_for group.group_messages.build do |f| %>
  <%= f.hidden_field 'group_id' %>
  <%= f.text_field 'content', placeholder: "Message #{group.name}" %><br>
  <%= f.submit %>
<% end %>

form_for([@group, @group_message]) do |f|

resources :groups do
  resources :group_messages
end
