<%= form_for([@found_group, @group_message]) do |f| %>
  <%= fields_for @group.group_messages.build do |message_fields|  %>
    <%= message_fields.text_field :content, placeholder: "Message #{@group.name}" %>
  <% end %>
  <%= f.submit 'Send' %>
<% end %>
